<%= form_with(model: api_poll_option, local: true) do |form| %>
  <% if api_poll_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_poll_option.errors.count, "error") %> prohibited this api_poll_option from being saved:</h2>

      <ul>
      <% api_poll_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
