<%= form_with(model: bid, local: true) do |form| %>
  <% if bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% bid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :belongs_to %>
    <%= form.text_field :belongs_to, id: :bid_belongs_to %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :bid_amount %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :bid_email %>
  </div>

  <div class="field">
    <%= form.label :xrp_address %>
    <%= form.text_field :xrp_address, id: :bid_xrp_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
