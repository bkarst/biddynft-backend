<%= form_with(model: nft, local: true) do |form| %>
  <% if nft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nft.errors.count, "error") %> prohibited this nft from being saved:</h2>

      <ul>
      <% nft.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :nft_title %>
  </div>

  <div class="field">
    <%= form.label :image_url %>
    <%= form.text_field :image_url, id: :nft_image_url %>
  </div>

  <div class="field">
    <%= form.label :supply %>
    <%= form.number_field :supply, id: :nft_supply %>
  </div>

  <div class="field">
    <%= form.label :author_image_url %>
    <%= form.text_field :author_image_url, id: :nft_author_image_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
