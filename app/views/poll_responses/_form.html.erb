<%= form_with(model: poll_response, local: true) do |form| %>
  <% if poll_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poll_response.errors.count, "error") %> prohibited this poll_response from being saved:</h2>

      <ul>
      <% poll_response.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
